<%= form_for(@contact) do |form| %>
  <table class="form">
    <tr>
	  <td class="label">Contact's Name:</td>
 	  <td><%= form.text_field(:name) %></td>
    </tr>
  </table>
  <%= form.submit %>
<% end %>